NEXT_PUBLIC_API_URL=http://localhost:8080
# Prices are displayed in INR (see src/lib/constants.ts)
# Must be the same key id as backend razorpay.key (secret stays on backend only)
NEXT_PUBLIC_RAZORPAY_KEY=rzp_test_your_key_id
